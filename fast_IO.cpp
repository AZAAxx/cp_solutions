#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
	cin.tie(nullptr);

	freopen("../in", "r", stdin);
	freopen("../out", "w", stdout);

    //use \n for new line unless you need to flush output
    cout << "example output" << "\n"; 
}
